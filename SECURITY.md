# Security

This app is designed as a software engineering demo project with fictional seed
data. It is not hardened for real deployment. The deliberately simple demo
credentials are public and must not be reused for a real system. Do not use the
app to store real names, email addresses, student records, or credentials.
